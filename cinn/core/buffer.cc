#include "cinn/core/buffer.h"

namespace cinn {

/*
ir::Expr Buffer::size() const {
  Expr result(primitive_bytes(ptype_));
  for (auto& dim : dims_) {
    result = result * dim;
  }
  return result;
}
 */

}  // namespace cinn
